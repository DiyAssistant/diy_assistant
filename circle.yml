dependencies:
  pre:
    - gem install rake
    - bundle install
    - rake db:create
    - rake db:migrate
    - rake db:migrate RAILS_ENV=test
    - rake db:seed
    - rake db:seed RAILS_ENV=test
    - rake db:reset
    - rake db:reset RAILS_ENV=test
