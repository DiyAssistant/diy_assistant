<%= form_for @instruction do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :description %>
  <%= f.text_area :description %>
  <%= f.collection_select(:category_id, @categories, :id, :name) %>
  <br>
  <%= f.submit 'Publish' %>
<% end %>
